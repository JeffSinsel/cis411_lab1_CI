# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
orbs:
  node: circleci/node@5.1.0
  docker: circleci/docker@2.2.0
  aws-ecr: circleci/aws-ecr@8.1.2 
  aws-ecs: circleci/aws-ecs@3.2.0 
  aws-cli: circleci/aws-cli@3.1.1 
# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          command: yarn run test
          name: Run tests
  deploy:
    executor: docker/docker
    steps:
      - checkout
      - aws-cli/install
      - aws-cli/setup
      # - run:
      #     name: Open port
      #     command: |
      #       PUBLIC_IP=$(curl ipinfo.io/ip)
      #       aws ec2 authorize-security-group-ingress --region $AWS_REGION --group-id $SG_ID --protocol tcp --port 22 --cidr $PUBLIC_IP/24
      #       sleep 5
      - run: 
          name: Connect to SSH server
          command: |
              echo ~/project ls
              ssh -i ~/project/lab1_keys.pem ec2_user@ec2-18-234-169-0.compute-1.amazonaws.com "echo Hello"
            # "sudo service docker start
            # docker pull 302847014979.dkr.ecr.us-east-1.amazonaws.com/lab1test:latest
            # docker run -p 4000:4000  --rm 302847014979.dkr.ecr.us-east-1.amazonaws.com/lab1test:latest"
      # - run:
      #     name: Deploy
      #     command: |
      #         sudo service docker start \
      #         docker pull 302847014979.dkr.ecr.us-east-1.amazonaws.com/lab1test:latest \
      #         docker run -p 4000:4000  --rm 302847014979.dkr.ecr.us-east-1.amazonaws.com/lab1test:latest
workflows:
  say-hello-workflow:
    jobs:
      # - test
      # - aws-ecr/build-and-push-image:
      #     executor: docker/docker
      #     repo: lab1test
      #     setup-remote-docker: true
      - deploy