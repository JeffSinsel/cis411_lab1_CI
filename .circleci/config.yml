# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
orbs:
  node: circleci/node@5.1.0
  docker: circleci/docker@2.2.0
  aws-ecr: circleci/aws-ecr@8.1.2 
  aws-ecs: circleci/aws-ecs@3.2.0 
  aws-cli: circleci/aws-cli@3.1.1 
# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          command: yarn run test
          name: Run tests
  deploy:
    machine:
      enabled: true
    steps:
      - checkout
      - aws-cli/install
      - aws-cli/setup
      # - run: 
      #     name: Setup key
      #     command: |
      #         echo "-----BEGIN RSA PRIVATE KEY-----\n" >> cis411_lab1.pem
      #         echo $SSH_KEY | tr " " "\n" >> cis411_lab1.pem
      #         echo "\n-----END RSA PRIVATE KEY-----" >> cis411_lab1.pem
      - run: chmod 400 cis411_lab1.pem
      - run: 
          name: Connect to SSH server
          command: |
              ssh -i "cis411_lab1.pem" ec2-user@ec2-100-25-33-244.compute-1.amazonaws.com " 
              sudo service docker start
              docker pull jeffsinsel/lab1:latest
              if [ docker ps -aq ]; then
                  docker run -p 4000:4000 --rm jeffsinsel/lab1:latest
              else
                  docker stop $(docker ps -aq)
                  docker run -p 4000:4000 --rm jeffsinsel/lab1:latest
              fi
              logout"
      # - run:
      #     name: Deploy
      #     command: |
      #         sudo service docker start \
      #         docker pull 302847014979.dkr.ecr.us-east-1.amazonaws.com/lab1test:latest \
      #         docker run -p 4000:4000  --rm 302847014979.dkr.ecr.us-east-1.amazonaws.com/lab1test:latest
workflows:
  say-hello-workflow:
    jobs:
      # - test
      # - aws-ecr/build-and-push-image:
      #     executor: docker/docker
      #     repo: lab1test
      #     setup-remote-docker: true
      - deploy